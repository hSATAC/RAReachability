Pod::Spec.new do |s|
  s.name         = "RAReachability"
  s.version      = "1.0"
  s.summary      = "Yet another SystemConfiguration Reachability Wrapper."
  s.homepage     = "https://github.com/evadne/RAReachability"
  s.author       = { "Evadne Wu" => "http://twitter.com/evadne" }
  s.source       = { :git => "https://github.com/evadne/RAReachability" }
  s.ios.deployment_target = '6.0'
  s.source_files = 'RAReachability'
end
